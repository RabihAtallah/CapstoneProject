For this project, I used datasets from data.medicare.gov which are publically available.
I did the data wrangling with python Jupyter notebook.
I included the raw datasets in the repository as csv files.
